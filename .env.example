# Example environment variables for Franklin Lap Counter
# Copy this file to .env and modify as needed

# Rust build configuration
RUST_VERSION=1.91
RUST_PI_TARGET=aarch64-unknown-linux-gnu
CONTAINER_IMAGE=franklin-rust-pi-builder

# Raspberry Pi deployment settings
PI_HOST=raspberrypi.local
PI_USER=pi
PI_DEST_DIR=/home/pi/franklin-lap-counter

# Pi setup options (used by setup-pi-advanced.sh)
# PI_HOST=192.168.1.100  # Use IP instead of hostname if needed
# PI_USER=myuser         # If you use a different username

# Optional: Custom build settings
# RUST_ARCH=aarch64-unknown-linux-gnu
# CONTAINER_REGISTRY=your-registry.com/username
